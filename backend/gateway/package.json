{
  "name": "gateway",
  "version": "1.0.0",
  "description": "GraphQl gateway for backend microservices",
  "scripts": {
    "build": "tsc -p ./",
    "start": "node dist/gateway.js",
    "dev": "nodemon -L --exec npx ts-node src/gateway.ts",
    "test": "nodemon -L --exec npx ts-node src/gateway.ts"
  },
  "author": "phoenixbeats01",
  "license": "ISC",
  "dependencies": {
    "@apollo/gateway": "^2.0.1",
    "apollo-server": "^3.6.7",
    "apollo-server-errors": "^3.3.1",
    "firebase-admin": "^10.1.0",
    "graphql": "^16.3.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.24",
    "apollo-server-types": "^3.5.2",
    "dotenv": "^16.0.0",
    "nodemon": "^2.0.15",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  }
}
